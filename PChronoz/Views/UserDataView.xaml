<UserControl x:Class="PChronoz.Views.UserDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PChronoz.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*" MaxWidth="700"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding SelectedUser}" Margin="0,100,0,0" Grid.Row="0" Style="{StaticResource TextBlockStyle}"/>

            <TextBlock Text="{Binding SelectedUserWins}" Foreground="LightGreen" Margin="0,50,0,0" Grid.Row="1" Style="{StaticResource TextBlockStyle}"/>
            
            <TextBlock Text="{Binding SelectedUserLoses}" Foreground="Crimson" Margin="0,50,0,0" Grid.Row="2" Style="{StaticResource TextBlockStyle}"/>

            <Button Content="Cartas" Grid.Row="3" Style="{StaticResource ControlButton}" Margin="0,30,0,0" Command="{Binding ChangeViewCommand}" CommandParameter="Cards"/>
        </Grid>

        <Border BorderBrush="White" BorderThickness="3" Margin="20,50,100,0" Grid.Row="0" Grid.Column="1">
            <Image Source="{Binding ProfilePhoto}" Margin="15" MaxHeight="750"/>
        </Border>
    </Grid>
</UserControl>
